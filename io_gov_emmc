!module
class=io

!view
view=title
text=(Storage I/O) I/O Scheduler
@show
echo true
@show
!view

!view
view=subtitle
text=(EMMC)Configure I/O Scheduler
@show
echo true
@show
!view

!view
view=spinner
text=Scheduler
@show
if [ -e /sys/block/mmcblk0/queue/scheduler ]
then
echo true
fi
@show
core=true
item_file_io=/sys/block/mmcblk0/queue/scheduler
@done
echo $label > /sys/block/mmcblk0/queue/scheduler
echo @toast=Successfully applied.
@done
@current
echo "io_label=`cat /sys/block/mmcblk0/queue/scheduler`"
@current
root=true
onboot=true
!view


!module
